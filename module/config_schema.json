{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "media_playback": {
            "required": false,
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "inline_video": {
		    "required": true,
		    "type": "boolean",
		    "description": "set this to 'true' to force webview to playivideo inline rather than launch a widget"
		}
                "require_user_action": {
		    "required": true,
		    "type": "boolean",
		    "description": "set this to true to require user interaction before playing media"
		}
		
            }
        }
    }
}
